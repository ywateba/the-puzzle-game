import React from 'react';
import GameBoard from 'common/src/components/GameBoard';
import { View, Text } from 'react-native';

const Game = () => (
  <View>
    <Text>Game Page</Text>
    <GameBoard />
  </View>
);

export default Game;
